#ifndef STALIN_H
#define STALIN_H

#include <stddef.h>

// stalin_sort sorts arr in-place, removing elements that break ascending order.
// Returns the new length of the array.
size_t stalin_sort(int *arr, size_t len);

#endif
